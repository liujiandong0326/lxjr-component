module.exports = {
  // 是否使用单引号代替双引号
  singleQuote: true,
  // 不尾随分号
  semi: false,
  // 包裹文字时候结束标签的结尾尖括号掉到了下一行
  htmlWhitespaceSensitivity: 'ignore',
  // 多行是否在最后添加逗号
  trailingComma: 'all',
  arrowParens: 'avoid',
}
